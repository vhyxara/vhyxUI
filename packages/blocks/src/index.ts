/**
 * @vhyxui/blocks — larger building blocks and prebuilt page layouts, composed
 * exclusively from @vhyxui/react primitives and components.
 *
 * Layering (DECISION-UI-002):
 *   @vhyxui/tokens  → design tokens (CSS)
 *   @vhyxui/react   → small, accessible components + layout primitives
 *   @vhyxui/blocks  → blocks (sections, forms, tables) and layouts (shells, pages)
 */
export * from './blocks';
export * from './layouts';
